// Thin shim, same pattern as lib/concepts.ts and lib/projects.ts: the source
// of truth is lib/content/failures/, one module per failure lab.
export { failures, failureCategories } from "./content/failures"
